name: Process
on:
  schedule:
    - cron: "05 03 * * *"
    - cron: "05 11 * * *"
  workflow_dispatch:

env:
  # time zone
  TZ: Asia/Shanghai

  # network proxy aggregate config
  SUBSCRIBE_CONF: ${{ secrets.SUBSCRIBE_CONF }}

  # token
  PUSH_TOKEN: ${{ secrets.PUSH_TOKEN }}

  # My Gist credentials
  MY_GIST_USERNAME: ${{ secrets.MY_GIST_USERNAME }}
  MY_GIST_ID: ${{ secrets.MY_GIST_ID }}

  # network reachable
  REACHABLE: ${{ vars.REACHABLE }}

  # not check connective
  SKIP_ALIVE_CHECK: ${{ vars.SKIP_ALIVE_CHECK }}

  # skip remark
  SKIP_REMARK: ${{ vars.SKIP_REMARK }}

  # workflow mode
  WORKFLOW_MODE: ${{ vars.WORKFLOW_MODE }}

  # include spwcial protocols, such vless hysteria2 and hysteria
  ENABLE_SPECIAL_PROTOCOLS: ${{ vars.ENABLE_SPECIAL_PROTOCOLS }}

jobs:
  process:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Prepare
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
          architecture: "x64"
          cache: "pip"

      - name: Install
        run: pip3 install -r requirements.txt

      - name: Replace Secrets in Config
        run: |
          echo "🔐 Replacing secrets in config file..."
          sed -i "s/{{MY_GIST_USERNAME}}/${{ secrets.MY_GIST_USERNAME }}/g" subscribe/config/my-process.json
          sed -i "s/{{MY_GIST_ID}}/${{ secrets.MY_GIST_ID }}/g" subscribe/config/my-process.json
          echo "✅ Secrets replaced successfully"

      - name: Process
        run: python -u subscribe/process.py -s subscribe/config/my-process.json --overwrite

      - name: Fix YAML Format
        run: |
          echo "🔧 Fixing YAML format issues..."
          
          # 查找所有生成的 YAML 文件
          file_count=0
          fixed_count=0
          
          for file in $(find . -type f \( -name "*.yaml" -o -name "*.yml" \)); do
            file_count=$((file_count + 1))
            echo "📄 Processing: $file"
            
            # 检查文件是否包含问题格式
            if grep -q "!<str>\|!<int>\|!<bool>\|!<float>" "$file"; then
              # 修复各种 YAML 类型标签
              sed -i 's/: !<str> /: /g' "$file"
              sed -i 's/: !<int> /: /g' "$file"
              sed -i 's/: !<bool> /: /g' "$file"
              sed -i 's/: !<float> /: /g' "$file"
              fixed_count=$((fixed_count + 1))
              echo "✅ Fixed: $file"
            else
              echo "✓ No issues found: $file"
            fi
          done
          
          echo ""
          echo "📊 Summary:"
          echo "   Total files processed: $file_count"
          echo "   Files fixed: $fixed_count"
          echo "✨ Format fix completed!"

      - name: Timestamp
        run: date
